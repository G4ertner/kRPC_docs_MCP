This package will contain JSON schema files in Phase A2.

Planned:
- `snippet.schema.json` with fields: id, repo, commit, path, lang, name, description, code, categories[], dependencies[], license, license_url, created_at.

